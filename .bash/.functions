function mkd () {
	if [ $# != 1 ]; then
		echo "Usage: mkd <dir>"
	else
		mkdir -pv "$1" && cd "$1"
	fi
}

function cd () {
	if [ -n "$1" ]; then
		builtin cd "$@" && ll
	else
		builtin cd ~ && ll
	fi
}

function gclone () {
    git clone --recurse-submodules --remote-submodules -j8 git@git-rhino:rhinocerose/"$1"
}

function grhino () {
    git clone git@git-rhino:rhinocerose/"$1"
}

function gadd () {
    git submodule add git@git-rhino:rhinocerose/"$1"
}
